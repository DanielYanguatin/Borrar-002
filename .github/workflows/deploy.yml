name: CI-Angular

on:
  push:
    branches:
      - main

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - name: Inicio de sesion en el repositorio.
        run: |
          echo: "Hola mundo 1"
          echo: "Hola mundo 2"
          